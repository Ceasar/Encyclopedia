
.. include:: index

================================================================================
Icon
================================================================================

An icon is an intentional indexical sign_; an icon is a representation or
likeness.

An icon resembles its object in some way.[1]
An icon bears a resemble to its object.[1]

Example: a bathroom sign
Example: portraits
Example: caricatures
Example: realistic painting
Example: photo
Example: geometric diagram
Example: models
Example: a map
Example: a bathroom sign
Example: onomatopoeia
Example: artificial flavor
Example: artificial scent
Example: small roman numbers "II"

The degree of representation (or abstractness) is variable.

Example: a photo of a cat
Example: a cartoon of a cat

Abstract icons describe more objects than concrete icons.

Icons are largely independent of cultural convention and may be described as
universal.
